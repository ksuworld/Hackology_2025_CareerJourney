<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CareerJourney">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCfDB7cycFm8FDf2PsixZhuYCEDDaVe7mo" />

        <!--            android:name=".MainActivity"-->
        <!--            android:name=".CustomGoogleMapsActivity"-->
        <!--            android:name=".CustomImageActivity"-->
        <!--            android:name=".VectorDrawableActivity"-->
        <activity
            android:name=".CanvasActivity"
            android:exported="true"
            android:theme="@style/Theme.CareerJourney">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
        <activity
            android:name=".onboarding.OnBoardingActivity"
            android:exported="true"
            android:theme="@style/Theme.CareerJourney">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>